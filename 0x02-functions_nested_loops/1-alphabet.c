#include "main.h"

/**
 * print_alphabet - prints alphabets in lowercase
 *
 * Return: Always 0 (Success)
 *
 */

void print_alphabet(void)
{

	int q;

	for (q = 'a'; q <= 'z'; q++)
	{
		_putchar(q);
	}
		_putchar('\n');

}
