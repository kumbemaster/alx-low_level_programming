#include "main.h"

/**
 * -abs - function that computes the absolute value of an integer
 *@int: this has to be a positive number
 * Return: Always 0 (Success)
 */

int _abs(int n)
{


	if (n >= 0)

		return (n);

	else

		return (-n);

}
